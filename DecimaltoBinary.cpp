#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    // Decimal to Binary
    /*
    int n, ans = 0;
    cin >> n;
    for (int i = 0; n != 0; i++)
    {
        int bit = n & 1;
        ans = (bit * pow(10, i)) + ans;
        n >>= 1;
    }
    cout << "Decimal to Binary = " << ans;

    */

   //Binary to Decimal 
    // int n, ans = 0;
    // cin >> n;
    // for (int i = 0; n != 0; i++)
    // {
    //     int bit = n % 10;
    //     if (bit == 1)
    //     {
    //         ans = ans + pow(2, i);
    //     }
    //     n /= 10;
    // }
    // cout << "Binary to Decimal = " << ans;



}